# Backend Dapur Ina Aina

Backend REST API berbasis Laravel. Penjelasan alur sistem, tabel database, dan daftar endpoint ada di [dokumentasi proyek](../DOKUMENTASI.md).

Jalankan API dari folder `backend` dengan `php artisan serve`. Pastikan MySQL aktif dan `.env` menunjuk ke database `dapur_aina`.
